using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConstantSideScrollMovement : MonoBehaviour
{
    
    //Apply constant torques to both wheels and increase them with time.
    //So that the car start moving faster
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
